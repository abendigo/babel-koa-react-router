{
  "name": "routes",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "babel": {
    "blacklist": [
      "regenerator"
    ]
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

    "build:vendor.js": "browserify -r react -r react-dom -r react-router -r history > dist/vendor.js",
    "build:bundle.js": "browserify --no-bundle-external dist/browser.js > dist/bundle.js",
    "build:compile": "babel src --out-dir dist --source-maps",

    "prebuild": "npm run clean",
    "build": "npm run build:compile && npm run build:vendor.js && npm run build:bundle.js",

    "clean": "rimraf dist",

    "start": "node dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.8.23",
    "browserify": "^11.2.0",
    "history": "^1.12.5",
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "koa": "^1.1.0",
    "koa-static": "^1.5.0",
    "react": "^0.14.0",
    "react-dom": "^0.14.0",
    "react-router": "^1.0.0-rc3"
  }
}
